import 'package:flutter/material.dart';

class AddTage extends StatelessWidget {
  const AddTage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Tag')),
      body: const Center(child: Text('Add Tag Screen')),
    );
  }
}
